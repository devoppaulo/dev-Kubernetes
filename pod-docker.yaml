apiVersion: v1
kind: Pod
metadata:
  name: my-app
  labels:
    app: web
spec:
  containers:
    - name: front-end
      image: paulogugas/devops22-docker:v1 
      ports:
        - containerPort: 80
  imagePullSecrets:
    - name: accesstodocker
    
